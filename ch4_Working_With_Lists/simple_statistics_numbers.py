# A few Python functions are helpful when working with lists of numbers --> Min, Max, Sum

digits = range(0, 10)

# for value in digits:
#     print(value)

print(min(digits))
print(max(digits))
print(sum(digits))

# In Terminal
# >>> digits = range(0, 10)
# >>> min(digits)
# 0
# >>> max(digits)
# 9
# >>> sum(digits)
# 45
# >>>
