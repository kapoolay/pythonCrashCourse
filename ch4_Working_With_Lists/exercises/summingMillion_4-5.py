# Make a list of the numbers one to one million
# Use min() and max() to make sure your list starts at one and ends at one million
# Use sum() function to see how quickly Python can add a million numbers

million = range(1,1000001)
print(min(million))
print(max(million))
print(sum(million))
