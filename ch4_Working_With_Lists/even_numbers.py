# Python can skip numbers in a given range. Pass a 3rd argument to range(), and Python uses that value as a step size when generating numbers

even_numbers = list(range(2, 11, 2))
print(even_numbers)
